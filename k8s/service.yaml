apiVersion: v1
kind: Service

metadata:
  name: viettech-app-service

spec:
  type: NodePort

  selector:
    app: viettech-app

  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
      nodePort: 30080